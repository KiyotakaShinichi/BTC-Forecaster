# Verified-compatible dependency set for the Track A quantitative core.
#
# This is NOT a full lockfile (no transitive closure, no hashes). It records the
# exact versions the platform was developed and tested against on
# CPython 3.14 / Windows, so a run can be reproduced or a regression bisected.
#
# Usage:
#   pip install -e ".[all]" -c constraints.txt
#
# Deliberately NOT upgraded as part of Track A. The committed research artifacts
# under research/runs/ were produced under these versions; bumping them is a
# separate, evidence-producing change.
#
# Notable: this environment runs pandas 3.x and numpy 2.x, which are newer than
# the >=2.0 / >=1.24 floors in pyproject.toml. The floors stay low because the
# code does not depend on 3.x-only behaviour; the pins here are what was tested.

# --- core quantitative stack ---
numpy==2.4.3
pandas==3.0.1
scipy==1.17.1
statsmodels==0.14.6

# --- models ---
prophet==1.3.0
xgboost==3.2.0
scikit-learn==1.8.0
arch==8.0.0

# --- data ---
yfinance==1.2.0

# --- plots ---
matplotlib==3.10.8

# --- api ---
fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.12.5
starlette==0.52.1

# --- cloud ---
boto3==1.43.82

# --- dev ---
pytest==8.4.1
