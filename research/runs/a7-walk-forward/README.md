# A7 walk-forward robustness -- run evidence

**Decision: `ROBUSTLY_UNINTERESTING`.** Scientific status: EXPLORATORY. No model is promoted and live trading stays disabled; A7 has no state that could do otherwise.

Generated by `python -m btc_forecaster.research.walk_forward`. Nothing in this directory is written by hand. Methodology: [`docs/walk-forward.md`](../../../docs/walk-forward.md).

## What was asked

- Models: `naive_last_value`, `random_walk_drift`, `ar_p`, `arima`, `theta`, `local_level`, `local_linear_trend`, `xgboost`, `ridge`, `mlp`, `lstm`
- Horizons: 1, 3, 7, 30 bars; target `ln(close[t + h] / close[t])`, natural-log return over h bars (dimensionless)
- Training windows: rolling-250, rolling-500, rolling-1000, rolling-2000, expanding
- Origins: 1417 daily origins, 2022-09-21 to 2026-08-07, refit at 12 folds; blocks early, middle, late
- Configurations gated: 200

## The decision

- Candidates clearing every gate: **0**
- Fragile signals: **0**
- Statistically different from naive after BH: 103
- Statistically better: 0
- Practically useful: 0

Multiple testing: 200 comparisons at alpha 0.05; 113 raw rejections against 10.0 expected from noise; 0 better and 103 worse after Benjamini-Hochberg.

Gate failures across all configurations:

| gate | failed |
|---|---|
| `aggregate_skill` | 171 |
| `bh_better` | 200 |
| `late_block` | 186 |
| `fold_majority` | 193 |
| `practical` | 200 |
| `leakage` | 0 |
| `resources` | 0 |

## Input

- `BTC-USD` via yfinance, retrieved 2026-09-07T02:53:49.326583+00:00, 3536 bars 2017-01-01 to 2026-09-07
- Frame sha256 `39b93e34520a496692ccc2156b8fe5c88c88b31bd9e9903a6fad74f282004bd3`
- Market data retrieved from Yahoo Finance through yfinance. Yahoo's terms do not grant redistribution, so the snapshot, the row-level predictions and the realised returns are not committed. The hashes are, and they are what makes a rerun checkable.

## Reproducing and checking

- Result digest **`b3fba7227e82e13ab2c112cb44169857aeb567cf7cb2a6f375bd7792f0d13790`**, over `comparisons.json`, `config.json`, `decision.json`, `folds.json`, `gates.json`, `input_manifest.json`, `leakage.json`, `metrics.json`, `predictions.csv`, `schedule.json`.
- Timings, the environment and this README are outside it: they describe the machine and the moment, not the result.
- `python -m btc_forecaster.research.walk_forward verify <this directory>` recomputes every hash from disk.
- A rerun on an input with a different frame sha256 is a different experiment, and the command refuses to call it a reproduction.

## What this is not

- Not comparable to A2, which scored a 31-step price path in USD.
- Not a re-statement of A6. A6 fitted once on 1,000 rows and scored one block at h = 1; this re-asks across origins, horizons, windows and periods.
- Not a trading signal, under any decision.
