services:
  btc-forecast-api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: btc-forecast-api
    ports:
      - "8010:8010"
    environment:
      OUTPUT_DIR: /app/out
      PLOT_SHOW: "0"
      APP_PORT: "8010"
    volumes:
      - ./out:/app/out
